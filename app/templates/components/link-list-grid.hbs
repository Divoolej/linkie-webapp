  {{!-- required for resizing cards --}}
  <div class="grid-sizer"></div>

  {{#each links as |link|}}
    {{link-item color="blue" title=link.title description=link.description url=link.url}}
  {{/each}}
